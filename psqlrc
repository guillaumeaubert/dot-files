-- Report the time spent on each query.
\timing

-- Change prompt:
--   %[...]% changes it to bold black
--   %m is the host name
--   %/ is the database name
--   %# is >, unless you're a superuser (#)
--   %R is =, unless in single-line mode (^) or disconnected (!)
\set PROMPT1 '%[%m:%/%#%R]% '
