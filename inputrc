# Include the system defaults.
$include /etc/inputrc

# When the directory is a symlink, auto-append the trailing slash to avoid
# having to double-tab.
set mark-symlinked-directories on

# Ignore case when auto-completing.
set completion-ignore-case on

# Use vi mode.
set editing-mode vi
